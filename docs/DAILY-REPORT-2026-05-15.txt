================================================================
HECO Portal - Daily Work Report
Date: 15 May 2026
================================================================

Total commits today: 10
Status: All work committed locally on main branch, unpushed


1. PRICING APPROVAL WORKFLOW
   - Provider rates now require HCT Admin approval before going live
   - New rates stay invisible to travellers until approved
   - Edits keep the old rate live until admin approves the change
   - Admin can reject with a reason that is shown to the provider

2. ADMIN PENDING-RATES PAGE + SIDEBAR BADGE
   - New /pending-pricing page lists every pending rate
   - Shows old -> new diff for edits, NEW/EDIT tags
   - Approve and Reject buttons per row
   - Sidebar badge shows pending count at a glance

3. BOOKING EMAIL TO PROVIDER
   - Provider gets an email on every new booking automatically
   - Email includes trip id, traveller, date, room, quantity, status
   - No duplicate emails when a booking is re-saved
   - Email failures never block the booking

4. PER-(TIER x ROOM) PRICING FIX
   - Reverted yesterday's over-simplification
   - Hotel can now have Cat A Single (2 rooms @ Rs.4500) AND
     Cat A Double (3 rooms @ Rs.5500) as separate rows
   - Different inventory and price per room type at same tier

5. CALENDAR ROOM-AVAILABILITY POPOVER REWORK
   - Day cells now show only the date - clean and scannable
   - Room details appear in a floating popover on hover
   - Shows tier pill, room name, availability count, colour-coded
   - Smart positioning (flips above/below, escapes containers)
   - Capped height with internal scroll for long lists

6. SP DASHBOARD COLLAPSIBLE PRICING LIST
   - Services/Rooms/Pricing now grouped by Comfort Tier as
     collapsible accordion
   - Each tier shows room-type count + total inventory in header
   - All collapsed by default - card stays short

7. ADMIN BULK-DELETE BUG FIX
   - Admin Delete Selected on pricing rows now actually deletes
     (was silently no-op before)

8. LIVE TIER REFRESH + DUPLICATE WARNING
   - "Comfort Tiers Offered" auto-updates without page reload
   - Duplicate (tier, room) rows get a yellow warning chip

9. PRE-SHIP VERIFICATION
   - Ran automated agent across every provider flow
   - 47/47 backend tests passed, 6/6 page-render smoke passed
   - One bug caught and fixed (pending-pricing 500 error)


================================================================
END OF DAILY REPORT
================================================================
